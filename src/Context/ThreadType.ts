/**
 * An enum of the different thread types that exists on RBKweb
 */

export enum ThreadType {

    /** Thread is a normal thread */
    Normal,

    /** Thread is sticky ('Prioritert' in norwegian) */
    Sticky,

    /** Thread is an announcement thread ('Viktig' in norwegian) */
    Announcement
}
